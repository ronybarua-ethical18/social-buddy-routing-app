import { Typography } from '@material-ui/core';
import React from 'react';

const About = () => {
    return (
        <div>
            <Typography variant ="h2">About Section is Under Processing</Typography>
        </div>
    );
};

export default About;